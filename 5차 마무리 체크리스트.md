# 5차 마무리 체크리스트

> 목표는 완벽한 콘솔 채팅 서버가 아니라, Unity 게임과 서버 간 TCP 통신을 구현하는 것이다.
>
> Unity 연동 전에 서버가 쉽게 죽거나 패킷을 조용히 잃게 만드는 문제만 고친다.  
> 나머지는 6차에서 실제 게임을 붙이면서 필요한 순서대로 개선한다.

## Unity 전에 반드시 고칠 것

### 1. 수신 패킷 크기 제한

- [v] `ConnectionInfo.ReceiveAsync()`에서 `length < 0`인 패킷을 거부한다.
- [v] Payload 최대 크기를 정하고, 초과하는 패킷을 거부한다.
- [v] 잘못된 길이를 보낸 클라이언트만 연결 종료하고 서버는 계속 실행한다.

#### 이유

현재는 네트워크에서 받은 길이만큼 바로 배열을 만든다.  
음수나 지나치게 큰 길이가 들어오면 예외 또는 과도한 메모리 할당으로 서버가 영향을 받을 수 있다.

---

### 2. 종료된 연결이 연결 풀에 계속 남는 문제

- [v] 클라이언트 연결 종료 시 연결 관리 목록에서도 반드시 제거한다.
- [v] 또는 `ConnectionManager`를 제거하고 `ClientManager` 한 곳에서 연결 수를 관리한다.
- [v] 접속과 종료를 5번 이상 반복한 뒤에도 새 클라이언트가 접속되는지 확인한다.

#### 이유

현재 `ConnectionManager`의 `ConcurrentBag`에는 연결을 추가하는 동작만 있고 개별 제거가 없다.  
이대로라면 동시 접속자 수가 아니라 서버 실행 후 누적 접속 횟수 때문에 연결 제한에 걸릴 수 있다.

---

### 3. 송신 큐의 패킷 자동 폐기 제거

- [v] `BoundedChannelFullMode.DropOldest`를 제거한다.
- [v] 우선 `BoundedChannelFullMode.Wait` 또는 `UnboundedChannel`을 사용한다.
- [v] 여러 작업이 동시에 전송할 수 있으므로 `SingleWriter = false`로 변경한다.

#### 이유

현재 송신 큐가 차면 오래된 패킷을 아무 알림 없이 버린다.  
Unity에서 캐릭터 생성, 퇴장, 상태 변경 패킷이 유실되면 클라이언트마다 게임 상태가 달라질 수 있다.

## 여기까지만 하면 5차 완료

- [v] 패킷 길이 검증 완료
- [v] 종료된 연결 제거 완료
- [v] 송신 큐의 `DropOldest` 제거 완료
- [v] 클라이언트 2개로 연결, 채팅, 유니캐스트, 종료 재접속 확인
- [v] **5차 완료 땅땅**

## 6차 Unity 연동 중에 고칠 것

아래 항목은 Unity로 넘어가기 전에 끝낼 필요가 없다. 실제 게임에 필요한 순서대로 처리한다.

- 방 입장·퇴장·이동 로직 완성
- `roomUserId` 기반 유니캐스트 정리
- 사용자 이름과 명령어 입력 검증
- 연결 종료 및 예외 처리 구조 정리
- Payload별 세부 유효성 검사
- 자동 테스트 추가
- Unity에서 사용할 Protocol 코드 분리
- Unity 메인 스레드용 수신 이벤트 큐 구현
- 위치 동기화 및 게임 패킷 추가

이 항목들은 **6차 구현 항목**이지 **5차 완료 조건**이 아니다.
